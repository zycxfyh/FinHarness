# FinHarness Abstraction Inventory v2
# Schema: machine-normalized fields + human explanations separated.
# See docs/engineering/agentic-abstraction-principles.md for taxonomy.
schema: finharness.abstraction_inventory.v2
status: current
updated: '2026-07-10'
description: 'Stabilization-phase inventory mapping existing complex artifacts to their
  target abstraction layers. Each entry records current_form, target_layer,
  target_form, migration_path, delete_condition, risk_level, and risk_reason.

  '
principles_ref: docs/engineering/agentic-abstraction-principles.md
objects:
- name: Account
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  target_form: Account (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — core business fact
  risk_level: none
  risk_reason: none — this IS a business fact
- name: Position
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  target_form: Position (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — core business fact
  risk_level: none
  risk_reason: none
- name: Snapshot
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  target_form: Snapshot (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — evidence anchor
  risk_level: none
  risk_reason: none
- name: Liability
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  target_form: Liability (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — core business fact
  risk_level: none
  risk_reason: none
- name: FinancialGoal
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  target_form: FinancialGoal (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — core business fact
  risk_level: none
  risk_reason: none
- name: CashflowEvent
  current_form:
  - statecore_model
  - receipt_backed_object
  target_layer:
  - object
  target_form: CashflowEvent (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — core business fact
  risk_level: none
  risk_reason: none
- name: TaxEvent
  current_form:
  - statecore_model
  - receipt_backed_object
  target_layer:
  - object
  target_form: TaxEvent (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — core business fact
  risk_level: none
  risk_reason: none
- name: InsurancePolicy
  current_form:
  - statecore_model
  - receipt_backed_object
  target_layer:
  - object
  target_form: InsurancePolicy (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — core business fact
  risk_level: none
  risk_reason: none
- name: DocumentRef
  current_form:
  - statecore_model
  - receipt_backed_object
  target_layer:
  - object
  target_form: DocumentRef (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — core business fact
  risk_level: none
  risk_reason: none
- name: ReceiptIndex
  current_form:
  - statecore_model
  - read_index
  target_layer:
  - trace
  target_form: ReceiptIndex (remain as Object / Trace index)
  migration_path: none_needed
  delete_condition:
  - never — evidence lookup surface
  risk_level: none
  risk_reason: none
- name: Proposal
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  - workflow
  target_form: Proposal (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — governed decision record
  risk_level: none
  risk_reason: none — correct layer
- name: Attestation
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  - trace
  target_form: Attestation (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — evidence of review
  risk_level: none
  risk_reason: none
- name: ReviewEvent
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - trace
  target_form: ReviewEvent (remain as Object)
  migration_path: none_needed
  delete_condition:
  - never — review timeline
  risk_level: none
  risk_reason: none
- name: InvestmentPolicyStatement
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  - resource
  target_form: IPS (remain as Object; serve as Resource/Context too)
  migration_path: none_needed
  delete_condition:
  - never — policy anchor
  risk_level: none
  risk_reason: none — IPS IS a long-lived policy fact
- name: CapitalMandate
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  - permission
  target_form: CapitalMandate (remain; freeze expansion)
  migration_path: none_needed
  delete_condition:
  - never — authority anchor
  risk_level: low
  risk_reason: low — but freeze further field expansion
- name: AgentAuthorityGrant
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - permission
  target_form: AgentAuthorityGrant (remain; freeze expansion)
  migration_path: none_needed
  delete_condition:
  - never — tool auth anchor
  risk_level: low
  risk_reason: low — but freeze new grant types
- name: BrokerConnection
  current_form:
  - statecore_model
  - execution_fact
  target_layer:
  - object
  target_form: Canonical Execution Kernel broker-adapter binding fact
  migration_path: none_needed
  delete_condition:
  - never — canonical execution boundary fact
  risk_level: low
  risk_reason: low — live-shaped values remain simulated-only and capability-gated
  runtime_refs:
  - src/finharness/statecore/execution_models.py
- name: ExecutionAccount
  current_form:
  - statecore_model
  - execution_fact
  target_layer:
  - object
  target_form: Canonical Execution Kernel account fact
  migration_path: none_needed
  delete_condition:
  - never — canonical execution account identity
  risk_level: low
  risk_reason: low — current accounts are unfunded and have no external credential path
  runtime_refs:
  - src/finharness/statecore/execution_models.py
- name: OrderDraft
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  - execution_fact
  target_layer:
  - object
  target_form: OrderDraft (canonical execution entry fact)
  migration_path: none_needed
  delete_condition:
  - never — canonical positive execution entry point
  risk_level: none
  risk_reason: none — correct classical object and bounded command surface
  runtime_refs:
  - src/finharness/statecore/execution_models.py
  - src/finharness/execution/services.py
- name: PreTradeCheck
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  - deterministic_gate_result
  target_layer:
  - object
  - evaluator
  target_form: PreTradeCheck (canonical persisted execution gate result)
  migration_path: none_needed
  delete_condition:
  - never — canonical pre-trade decision evidence
  risk_level: none
  risk_reason: none — deterministic evaluator output is persisted as lifecycle evidence
  runtime_refs:
  - src/finharness/statecore/execution_models.py
  - src/finharness/execution/services.py
- name: ApprovalRecord
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  - human_decision_trace
  target_layer:
  - object
  - permission
  - trace
  target_form: ApprovalRecord (canonical human execution-decision evidence)
  migration_path: none_needed
  delete_condition:
  - never — canonical approval evidence
  risk_level: none
  risk_reason: none — records a human decision without granting broader authority
  runtime_refs:
  - src/finharness/statecore/execution_models.py
  - src/finharness/execution/services.py
- name: ExecutionOrder
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  - execution_fact
  target_layer:
  - object
  target_form: ExecutionOrder (canonical staged/submitted order fact)
  migration_path: none_needed
  delete_condition:
  - never — canonical execution lifecycle fact
  risk_level: none
  risk_reason: none — correct classical object behind capability and lifecycle gates
  runtime_refs:
  - src/finharness/statecore/execution_models.py
  - src/finharness/execution/services.py
- name: ExecutionReport
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  - execution_fact
  target_layer:
  - object
  - trace
  target_form: ExecutionReport (canonical broker/simulated outcome fact)
  migration_path: none_needed
  delete_condition:
  - never — canonical execution outcome evidence
  risk_level: none
  risk_reason: none — correct classical fact; current producers are simulated only
  runtime_refs:
  - src/finharness/statecore/execution_models.py
  - src/finharness/execution/services.py
- name: PositionDelta
  current_form:
  - statecore_model
  - receipt_backed_object
  - execution_fact
  target_layer:
  - object
  - trace
  target_form: PositionDelta (canonical post-execution change fact)
  migration_path: none_needed
  delete_condition:
  - never — canonical position lineage evidence
  risk_level: none
  risk_reason: none — correct immutable execution consequence fact
  runtime_refs:
  - src/finharness/statecore/execution_models.py
  - src/finharness/execution/services.py
- name: ReconciliationReport
  current_form:
  - statecore_model
  - receipt_backed_object
  - execution_fact
  target_layer:
  - object
  - evaluator
  - trace
  target_form: ReconciliationReport (canonical expected-vs-actual execution evidence)
  migration_path: none_needed
  delete_condition:
  - never — canonical reconciliation evidence
  risk_level: none
  risk_reason: none — correct deterministic comparison and trace artifact
  runtime_refs:
  - src/finharness/statecore/execution_models.py
  - src/finharness/execution/services.py
- name: ActionIntent
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  - action_chain_head
  target_layer:
  - context
  - workflow
  target_form: legacy_internal context/workflow artifact; execution facts are separated by Execution Legacy Bridge
  migration_path: no_new_callers_then_delete_writes
  delete_condition:
  - no direct API callers on old write path
  - Execution Legacy Bridge covers historical classification
  - historical receipts remain readable
  risk_level: high
  risk_reason: high — legacy chain head duplicates the canonical OrderDraft entry point
- name: ActionIntentAuthorityBinding
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - evaluator
  - permission
  - trace
  target_form: legacy_internal evaluator/permission trace; bridge classifies findings without creating a new execution object
  migration_path: no_new_callers_then_delete_writes
  delete_condition:
  - no direct API callers
  - bridge findings remain readable
  - historical receipts remain readable
  risk_level: high
  risk_reason: high — continues authority-chain object inflation; this is an evaluator finding pretending to be a business
    object
- name: ActionIntentSimulationReport
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - workflow
  - evaluator
  target_form: legacy_internal workflow output; execution-relevant findings project to canonical PreTradeCheck
  migration_path: bridge_existing_receipts_then_delete_writes
  delete_condition:
  - no direct API callers
  - bridge preserves execution findings and agentic narrative classification
  - historical receipts remain readable
  risk_level: moderate
  risk_reason: moderate — simulation is a workflow output, not a standalone business fact
- name: TradePlanCandidate
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - workflow
  target_form: legacy_internal planning workflow artifact; no canonical execution object
  migration_path: no_new_callers_then_delete_writes
  delete_condition:
  - no direct API callers
  - review history remains readable through legacy receipts
  - historical receipts remain readable
  risk_level: high
  risk_reason: high — trade plan object chain keeps growing; should be a section in aggregate view, not independent objects
- name: CapitalObjectiveFit
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - skill
  - resource
  target_form: legacy_internal review memo / skill output; never an execution fact
  migration_path: preserve_as_agentic_artifact_then_delete_writes
  delete_condition:
  - no direct API callers
  - review history remains readable through legacy receipts
  - historical receipts remain readable
  risk_level: high
  risk_reason: high — objective fit is a review judgment, not a standalone business fact
- name: TradePlanReviewGate
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - permission
  - trace
  target_form: legacy_internal review-gate trace; approved facts project to canonical ApprovalRecord
  migration_path: bridge_existing_receipts_then_delete_writes
  delete_condition:
  - no direct API callers
  - bridge preserves approval projection and historical trace
  - historical receipts remain readable
  risk_level: high
  risk_reason: high — review gate is a permission checkpoint, not a standalone object
- name: PaperOrderTicketCandidate
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  - trace
  target_form: legacy_internal order candidate; bridge projects execution facts to canonical OrderDraft and ExecutionOrder
  migration_path: bridge_existing_receipts_then_delete_writes
  delete_condition:
  - canonical Execution Kernel covers new simulated orders
  - no direct legacy write callers
  - historical receipts remain readable
  risk_level: high
  risk_reason: high — legacy order-shaped write duplicates canonical execution lifecycle
- name: PaperExecutionReceipt
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  - trace
  target_form: legacy_internal simulated outcome; bridge projects facts to canonical ExecutionReport
  migration_path: bridge_existing_receipts_then_delete_writes
  delete_condition:
  - canonical ExecutionReport covers new simulated outcomes
  - no direct legacy write callers
  - historical receipts remain readable
  risk_level: high
  risk_reason: high — legacy execution-shaped write duplicates canonical ExecutionReport
- name: PaperAccount
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  target_form: legacy_internal paper account compatibility fact; new work uses canonical ExecutionAccount and execution review projections
  migration_path: consumer_audit_then_read_only_compatibility
  delete_condition:
  - no direct legacy write callers
  - historical receipts remain readable
  risk_level: moderate
  risk_reason: moderate — compatibility account can be mistaken for a second execution ledger
- name: PaperPosition
  current_form:
  - statecore_model
  - receipt_backed_object
  - api_surface
  target_layer:
  - object
  - trace
  target_form: legacy_internal paper position compatibility fact; new review derives from canonical PositionDelta and ReconciliationReport
  migration_path: consumer_audit_then_read_only_compatibility
  delete_condition:
  - no direct legacy write callers
  - historical receipts remain readable
  risk_level: moderate
  risk_reason: moderate — compatibility position can be mistaken for canonical execution state
- name: AgentRunReceipt
  current_form:
  - receipt_only
  - trace_primitive
  target_layer:
  - trace
  target_form: AgentRunReceipt (remain receipt-only)
  migration_path: receipt_first
  delete_condition:
  - replaced by AgentRun table only after >=2 real integration points need query/state
  risk_level: low
  risk_reason: correct first trace primitive; do not graduate to StateCore prematurely
- name: ContextTrust
  current_form:
  - projection_metadata
  - context_annotation
  target_layer:
  - context
  - resource
  target_form: ContextTrust metadata on agent-facing context
  migration_path: projection_first
  delete_condition:
  - never — epistemic metadata remains required for agent cognition
  risk_level: low
  risk_reason: correct agent cognition metadata; next risk is pack-level coarseness
- name: EvaluationReport
  current_form:
  - projection_model
  - evaluator_common_shape
  target_layer:
  - evaluator
  target_form: EvaluationReport common projection
  migration_path: projection_first
  delete_condition:
  - replaced only if full evaluator runtime appears
  risk_level: low
  risk_reason: correct common evaluator shape; do not turn into governance decision
- name: AuthorityTransitionRecord
  current_form:
  - receipt_only
  - eligibility_record
  target_layer:
  - permission
  - trace
  target_form: eligibility-only authority transition record
  migration_path: receipt_first
  delete_condition:
  - never for recorded transitions; may graduate after real workflow consumers
  risk_level: moderate
  risk_reason: must not become execution authorization; eligibility-only boundary must hold
- name: PlanningPolicyView
  current_form:
  - read_model
  - feedback_projection
  target_layer:
  - feedback
  - context
  target_form: planner-readable view from traceable RuleChange
  migration_path: projection_first
  delete_condition:
  - replaced only by stronger planner policy runtime
  risk_level: low
  risk_reason: correct learning-to-context bridge; not planner runtime
- name: OptionSetReceipt
  current_form:
  - receipt_only
  - deliberation_artifact
  target_layer:
  - deliberation
  target_form: receipt-only option set
  migration_path: receipt_first
  delete_condition:
  - graduate only after multiple planning/review consumers
  risk_level: moderate
  risk_reason: avoid becoming proposal replacement or shadow action object
- name: PlanDraftReceipt
  current_form:
  - receipt_only
  - deliberation_artifact
  target_layer:
  - deliberation
  target_form: receipt-only plan draft
  migration_path: receipt_first
  delete_condition:
  - graduate only after real loop/evaluator consumers
  risk_level: moderate
  risk_reason: must not become execution plan or order generator
registries:
- name: DataCatalog
  current_form:
  - read_model
  - api_surface
  target_layer:
  - resource
  - context
  target_form: DataCatalog (remain as Resource)
  migration_path: none_needed — already correct
  delete_condition:
  - never — data trust anchor
  risk_level: none
  risk_reason: none — already a Resource
- name: RiskRegister
  current_form:
  - read_model
  target_layer:
  - resource
  - context
  target_form: RiskRegister (remain as Resource)
  migration_path: none_needed
  delete_condition:
  - never — risk awareness anchor
  risk_level: none
  risk_reason: none — already a Resource
- name: DebtRegister
  current_form:
  - registry
  - json_file
  target_layer:
  - loop
  - evaluator
  target_form: DebtRegister (remain; repurpose as Loop backlog)
  migration_path: none_needed — classification only
  delete_condition:
  - never — debt tracking is essential
  risk_level: low
  risk_reason: low — but don't let it grow into an object system
- name: SupportSurfaceRegistry
  current_form:
  - registry
  - yaml_file
  - machine_checked
  target_layer:
  - loop
  - resource
  target_form: SupportSurfaceRegistry (remain; downgrade from "current fact" stance)
  migration_path: none_needed — classification only
  delete_condition:
  - possibly downgrade to reference-only
  risk_level: low
  risk_reason: low — but don't let it expand into an object system
- name: GraphRegistry
  current_form:
  - registry
  - python_module
  target_layer:
  - evaluator
  - resource
  target_form: GraphRegistry (remain; downgrade)
  migration_path: none_needed — classification only
  delete_condition:
  - possibly downgrade to reference-only
  risk_level: low
  risk_reason: low — but don't expand
- name: PolicyRegistry
  current_form:
  - registry
  - python_module
  target_layer:
  - guardrail
  target_form: PolicyRegistry (remain as Guardrail registry)
  migration_path: none_needed
  delete_condition:
  - never — policy enforcement anchor
  risk_level: none
  risk_reason: none — already correct as guardrail
execution_artifacts:
- name: Execution Services
  current_form:
  - python_module
  - receipt_backed_commands
  - deterministic_lifecycle
  target_layer:
  - classical_service
  - workflow
  target_form: Canonical Execution Kernel command services (remain)
  migration_path: none_needed
  delete_condition:
  - never — canonical execution lifecycle implementation
  risk_level: none
  risk_reason: none — correct classical service boundary with semantic lifecycle tests
  runtime_refs:
  - src/finharness/execution/services.py
  - src/finharness/execution/receipts.py
- name: Execution Capabilities
  current_form:
  - frozen_policy_model
  - service_enforced_control
  target_layer:
  - classical_control
  - guardrail
  - permission
  target_form: Immutable service/command/API capability control (remain)
  migration_path: none_needed
  delete_condition:
  - never — fail-closed execution control boundary
  risk_level: none
  risk_reason: none — flags are enforced before effects and live/credential capabilities stay false
  runtime_refs:
  - src/finharness/execution/capabilities.py
  - src/finharness/api/dependencies.py
- name: BrokerAdapter Registry
  current_form:
  - protocol
  - in_process_registry
  - simulated_only_guard
  target_layer:
  - classical_adapter_boundary
  - guardrail
  target_form: BrokerAdapter protocol plus simulated-only registration policy (remain)
  migration_path: none_needed
  delete_condition:
  - never — canonical adapter isolation boundary
  risk_level: low
  risk_reason: low — any real adapter requires a separate C3 capability and threat model
  runtime_refs:
  - src/finharness/execution/broker.py
- name: SimulatedBrokerAdapter
  current_form:
  - python_adapter
  - local_simulation
  target_layer:
  - classical_adapter
  target_form: Canonical local simulated execution substrate (remain)
  migration_path: none_needed
  delete_condition:
  - never while local paper/simulated execution is supported
  risk_level: none
  risk_reason: none — no network, SDK, credential, funded account, or external venue
  runtime_refs:
  - src/finharness/execution/adapters/simulated_broker.py
- name: Execution Legacy Bridge
  current_form:
  - read_only_projection
  - migration_classifier
  target_layer:
  - migration_bridge
  - evaluator
  target_form: Read-only separation of legacy execution facts, agentic artifacts, and deletion candidates
  migration_path: shrink_with_legacy_surface
  delete_condition:
  - legacy writes removed
  - historical receipts remain readable without runtime projection
  risk_level: low
  risk_reason: low — bridge reads/classifies only and must not become a product API
  runtime_refs:
  - src/finharness/execution/legacy_bridge.py
api_routes:
- name: execution_routes
  current_form:
  - api_surface
  - read_and_write
  - canonical_execution
  target_layer:
  - classical_api
  target_form: Canonical /execution/* lifecycle routes (remain)
  migration_path: none_needed
  delete_condition:
  - never while Execution Kernel is canonical
  risk_level: none
  risk_reason: none — state changes require local write and execution capability gates
  runtime_refs:
  - src/finharness/api/routes_execution.py
- name: action_intent_routes
  current_form:
  - api_surface
  - write_routes
  target_layer:
  - permission
  - evaluator
  target_form: legacy_internal; superseded_by canonical /execution/* plus Execution Legacy Bridge
  migration_path: no_new_callers_then_delete_writes
  delete_condition:
  - no direct API callers
  - historical receipts remain readable
  risk_level: high
  risk_reason: high — deprecated legacy writes duplicate canonical execution concepts
- name: paper_validation_routes
  current_form:
  - api_surface
  - write_routes
  target_layer:
  - classical_api
  - trace
  target_form: legacy_internal compatibility routes; new simulated work uses canonical /execution/*
  migration_path: consumer_audit_then_delete_writes
  delete_condition:
  - no direct API callers
  - historical receipts remain readable
  - dedicated legacy isolation/deletion boundary approved
  risk_level: high
  risk_reason: high — order/account-shaped legacy writes can regrow a second execution system
- name: agent_authority_grant_routes
  current_form:
  - api_surface
  - write_routes
  target_layer:
  - permission
  target_form: remain; freeze expansion
  migration_path: none_needed
  delete_condition:
  - never — auth anchor
  risk_level: low
  risk_reason: low — freeze new grant types
- name: capital_mandate_routes
  current_form:
  - api_surface
  - write_routes
  target_layer:
  - resource
  target_form: remain; freeze expansion
  migration_path: none_needed
  delete_condition:
  - never — authority anchor
  risk_level: low
  risk_reason: low — freeze new mandate fields
- name: proposal_routes
  current_form:
  - api_surface
  - read_and_write
  target_layer:
  - resource
  target_form: remain
  migration_path: none_needed
  delete_condition:
  - never — core workflow surface
  risk_level: none
  risk_reason: none
- name: review_routes
  current_form:
  - api_surface
  - read_and_write
  target_layer:
  - resource
  target_form: remain
  migration_path: none_needed
  delete_condition:
  - never — core workflow surface
  risk_level: none
  risk_reason: none
- name: ips_routes
  current_form:
  - api_surface
  - write_routes
  target_layer:
  - resource
  target_form: remain; freeze expansion
  migration_path: none_needed
  delete_condition:
  - never — IPS anchor
  risk_level: low
  risk_reason: low
- name: data_catalog_routes
  current_form:
  - api_surface
  - read_only
  target_layer:
  - resource
  target_form: remain
  migration_path: none_needed
  delete_condition:
  - never — data trust surface
  risk_level: none
  risk_reason: none
- name: data_quality_routes
  current_form:
  - api_surface
  - read_only
  target_layer:
  - evaluator
  target_form: remain
  migration_path: none_needed
  delete_condition:
  - never — data trust surface
  risk_level: none
  risk_reason: none
- name: risk_routes
  current_form:
  - api_surface
  - read_only
  target_layer:
  - resource
  target_form: remain
  migration_path: none_needed
  delete_condition:
  - never — risk awareness surface
  risk_level: none
  risk_reason: none
- name: state_routes
  current_form:
  - api_surface
  - read_only
  target_layer:
  - resource
  target_form: remain
  migration_path: none_needed
  delete_condition:
  - never — state query surface
  risk_level: none
  risk_reason: none
- name: cockpit_routes
  current_form:
  - api_surface
  - read_only
  - frontend_static
  target_layer:
  - resource
  target_form: remain
  migration_path: none_needed
  delete_condition:
  - never — cockpit surface
  risk_level: none
  risk_reason: none
agent_artifacts:
- name: Agent Capabilities (agent_capabilities.py)
  current_form:
  - python_module
  - tool_registry
  target_layer:
  - tool
  - permission
  target_form: AgentCapabilities (remain; freeze new capability profiles)
  migration_path: none_needed
  delete_condition:
  - never — agent tool boundary
  risk_level: moderate
  risk_reason: moderate — freeze expansion; classify each capability as Tool/Resource/Evaluator
- name: Agent Runtime (agent_runtime.py)
  current_form:
  - python_module
  - loop_entrypoint
  target_layer:
  - workflow
  target_form: AgentRuntime (remain; classify as Workflow runner)
  migration_path: none_needed
  delete_condition:
  - never — agent execution host
  risk_level: low
  risk_reason: low
- name: Agent Tools (agent_tools.py)
  current_form:
  - python_module
  - tool_implementations
  target_layer:
  - tool
  target_form: AgentTools (remain; each tool individually classified)
  migration_path: none_needed
  delete_condition:
  - never — tool implementations
  risk_level: low
  risk_reason: low
governance_artifacts:
- name: change-control.md
  current_form:
  - documentation
  - process_rule
  target_layer:
  - guardrail
  - evaluator
  target_form: Guardrail doc + Evaluator test
  migration_path: none_needed — already correct
  delete_condition:
  - never — governance documentation
  risk_level: none
  risk_reason: none
- name: gate-checklists.md
  current_form:
  - documentation
  - checklist
  target_layer:
  - skill
  target_form: Skill (checklist form)
  migration_path: convert to .skills/verify-gate-checklist/
  delete_condition:
  - never — governance documentation
  risk_level: low
  risk_reason: low — static doc is fine but skill form enables agent-driven verification
- name: operating-model.md
  current_form:
  - documentation
  - process_model
  target_layer:
  - workflow
  target_form: remain
  migration_path: none_needed
  delete_condition:
  - never — governance documentation
  risk_level: none
  risk_reason: none
- name: Taskfile check
  current_form:
  - task_runner
  - build_script
  target_layer:
  - evaluator
  - loop
  target_form: Taskfile (remain; split into check / check:full / release:preflight)
  migration_path: split_later — PR-9
  delete_condition:
  - never — governance build script
  risk_level: low
  risk_reason: low — but too heavy for local iteration
